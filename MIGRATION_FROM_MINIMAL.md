# Перенос из минимальной версии

Из репозитория `GitDP-WKS/-` в основной репозиторий `GitDP-WKS/Night` нужно перенести следующие рабочие идеи:

1. `CONN` считать принятым по умолчанию вместе с `ANS`.
2. Рядом с KPI показывать формулу: `% пропущенных = ABAN / (ANS + CONN + ABAN)` по выбранным skills.
3. Добавить быстрый CSV-экспорт событий без предварительной подготовки Excel/Word.
4. Оставить резервный минимальный режим для быстрой проверки HTML-отчета Avaya.

Основной `app.py` в `Night` является рабочей расширенной версией, поэтому изменения лучше вносить точечно, без полной замены файла.